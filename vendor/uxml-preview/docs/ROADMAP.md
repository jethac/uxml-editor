# Roadmap

각 Phase의 완료 조건(DoD)에 전부 체크되기 전에는 다음 Phase로 넘어가지 않는다.
특히 **Phase 2를 건너뛰고 렌더링부터 만들고 싶은 유혹**이 강하게 오는데,
그러면 Phase 8에서 데이터 모델을 갈아엎게 된다.

```
Phase 0  저장소 뼈대 + 놀이터        │ 반나절
Phase 1  데이터 모델 설계             │ 1일      ← 가장 중요
Phase 2  파서 + 직렬화 (왕복 검증)    │ 2~3일    ← 관문
Phase 3  USS 스타일 리졸버            │ 2~3일
Phase 4  Yoga 레이아웃 + DOM 페인팅   │ 3~4일    ← 처음으로 화면에 나옴
Phase 5  골든 테스트 체계             │ 2일      ← 신뢰도의 근거
Phase 6  놀이터 완성 + 공개           │ 1~2일    ← v0.1
─────────────────── 여기까지가 v0.1 ───────────────────
Phase 7  컨트롤 확장
Phase 8  편집 레이어
Phase 9  외부 도구 통합
```

---

## Phase 0 — 저장소 뼈대와 놀이터

**목표:** `pnpm dev`로 빈 화면이라도 뜨는 상태.

- [x] TypeScript + Vitest + Vite 빌드가 돌아간다
- [x] `playground/`에 좌우 2분할 페이지가 뜬다
- [x] `pnpm test`가 통과한다 (테스트 0개여도 됨)
- [x] `CLAUDE.md`가 있다
- [x] `docs/uss-reference.md`에 참조표가 있다
- [x] `docs/backlog.md`, `docs/progress.md` 생성
- [x] Git 초기 커밋

**완료 (2026-08-02).** `pnpm build`가 `dist/index.js` + `dist/index.d.ts`를 낸다.
`index.js`는 아직 빈 파일이다 — 공개 API가 `export declare`라 런타임 코드가 없다.
Phase 2에서 실제 구현이 들어가면 채워진다.

> 참조표를 먼저 넣는 이유: CSS↔USS 매핑 표가 Phase 3·4의 사실상 명세서다.
> 이게 없으면 CSS 상식으로 추론해서 존재하지 않는 속성을 매핑한다.

---

## Phase 1 — 데이터 모델 설계

**목표:** 파서도 렌더러도 없지만, UXML 문서를 메모리에서 어떻게 표현할지 확정.
**이 Phase가 프로젝트 전체의 성패를 가른다.**

- [x] `src/model/types.ts`에 전체 타입 정의
- [x] 각 스타일 값의 **출처** 추적 가능 (인라인 / USS 규칙 / 상속 / 기본값) — `StyleOrigin`
- [x] 원본 텍스트 정보(주석, 속성 순서, 공백) 보존 자리 확보
- [x] 타입만 보고도 "이걸로 직렬화가 되겠다"는 확신

**완료 (2026-08-02).** 관통하는 원칙 하나로 정리됐다 —
**모델에는 파일에서 온 것만 넣고, 계산해서 알 수 있는 것은 넣지 않는다.**
계산된 스타일, "이 컨트롤을 지원하는가", 줄 번호, 펼친 shorthand가 전부 모델 밖이다.
파생물을 저장하면 편집 후 낡고, 낡은 걸 못 알아채면 틀린 것을 보여준다.

주석에는 전용 타입이 없다. 요소·규칙·선언 **사이의 공백을 원본에서 슬라이스**하는
방식이라 주석이 그 안에 실려 저절로 살아남는다. 루트 바깥(XML 선언, 첫머리 주석)도
같은 방식이다.

### Phase 2로 넘길 것

- 왕복 기준의 실측: 픽스처에서 바이트 일치가 실제로 나오는지 확인한다.
  스팬 재출력이 맞다면 편집 없는 왕복은 정의상 바이트 일치여야 한다
- `tagDirty` / `childrenDirty` 분리가 실제 편집에서 의도대로 도는지 검증

> 출처 정보가 필요한 이유: 사용자가 화면에서 색을 바꿨을 때
> "인라인에 쓸지, USS 규칙을 고칠지"를 이 정보로 판단한다.
> Phase 8에서 이게 없으면 편집 기능을 만들 수 없다.

---

## Phase 2 — 파서 + 직렬화 (왕복 검증) ★ 관문

**목표:** 파일을 읽었다가 그대로 다시 쓰면 원본과 같아지는 상태.
화면에는 아무것도 안 나온다.

- [x] 테스트 픽스처 10개 이상 (주석 · 속성 순서 · CRLF · 미지원 컨트롤 포함) — 15개
- [x] UXML 스캐너 — 요소 종류를 구분하지 않고 전부 읽는다
- [x] USS 파서 (셀렉터 + 선언)
- [x] UXML/USS 직렬화 — clean 노드는 원본에서 슬라이스
- [x] **왕복 테스트** — 편집 없는 왕복은 바이트 일치 (15/15)
- [x] **구조 테스트** — 트리와 규칙이 실제로 맞는지 (33개)
- [x] **편집 테스트** — 고친 영역만 바뀌는지 (14개)
- [x] 주석이 살아남음
- [x] 속성 순서가 유지됨

**완료 (2026-08-02).** 테스트 65개. 바이트 일치가 첫 실행에 15/15로 나왔다 —
스팬 재출력 설계가 실제로 작동한다.

### 알려진 한계 (Phase 8 전에 손봐야 할 것)

- **속성 하나를 고치면 그 태그의 속성 전부가 재생성된다.** `tagDirty`가 태그
  단위라서다. 여러 줄에 걸친 속성이 한 줄로 접힌다. 태그 바깥은 안 건드리므로
  치명적이진 않지만 diff가 필요 이상으로 크다. 자식에 쓴 간격 슬라이스 기법을
  속성에도 적용하면 된다 (`Attribute`에 `dirty` 추가)
- **`childrenDirty`가 켜지면 자식 사이의 주석이 사라진다.** 주석은 간격 안에서만
  살아남는데, 자식 목록이 바뀌면 그 간격이 무효가 되기 때문이다. 편집이 없는
  동안은 문제가 없지만, Phase 8은 간격을 형제로 승격시켜야 한다

> **왕복 테스트만으로는 파서가 옳다는 것이 증명되지 않는다.**
> 스팬으로 슬라이스해 뱉으면 트리가 엉망이어도 원본이 그대로 재현된다.
> 왕복이 검증하는 것은 스팬 장부가 파일을 빠짐없이 덮는가일 뿐이다.
> 트리가 맞는지는 구조 테스트로 따로 봐야 한다. 이 둘을 뭉치면
> Phase 3에서 셀렉터가 안 맞는 이유를 찾느라 파서를 다시 읽게 된다.

> 바이트 일치는 목표가 아니라 **결과**다. 스팬 재출력이면 편집 없는 왕복은
> 정의상 바이트 일치이고, 안 나오면 파서가 스팬을 잘못 잡은 것이다.
> (Phase 1 이전에는 이 기준을 "git diff가 깨끗한가"로 낮춰 잡았으나,
> 스팬 재출력을 택하면서 낮출 이유가 없어졌다.)

---

## Phase 3 — USS 스타일 리졸버

**목표:** "이 요소에 최종적으로 어떤 스타일이 적용되는가"를 계산.

- [x] 셀렉터 매칭 (타입 / 클래스 / 이름 / 와일드카드 / 자손 / 자식 / 복합)
- [x] `@import` 처리 — `parse`에 `resolveImport` 훅. 미해결이면 경고
- [x] 미지원 셀렉터를 경고로 처리하고 해당 규칙만 무시
- [x] 우선순위 계산이 Unity 규칙과 일치 — 문서화된 **순서**와는 일치. 아래 참조
- [x] 상속 처리
- [x] `var()` 커스텀 속성
- [x] 각 스타일 값에 출처 기록 — `StyleOrigin`
- [x] 우선순위 테스트 20개+ — 캐스케이드 테스트 50개

**완료 (2026-08-02).** 테스트 117개.

### 검증되지 않은 가정 — Phase 5에서 실측한다

- **우선순위 계산 방식.** 순서(`#name` > `.class` > 타입 > `*`, 동률이면 뒤가 승)는
  문서화돼 있지만, **트리플로 세는 방식과 pseudo-class를 class로 세는 규칙은
  그 순서에서 역산한 것**이다. 유니티에서 확인한 적이 없다.
  Phase 5 골든 테스트가 동률 처리에서 어긋나면 `src/style/specificity.ts`를 먼저 본다
- **상속 목록.** UI Toolkit이 버전마다 넓혀와서 버전 의존이다.
  `src/style/properties.ts`의 `INHERITED` 한 곳에만 있다
- **`:root`.** USS의 `:root`는 CSS와 달리 스타일시트가 적용된 요소를 가리킨다.
  프리뷰에는 UIDocument가 없어서 `<ui:UXML>`에 매칭시켰다 — 디자인 토큰 패턴이
  깨지지 않는 유일한 해석이다. 만날 때마다 `version-dependent` 경고를 남긴다

### 계획에 없었지만 넣은 것

- **shorthand 전개를 여기서 한다.** ROADMAP에는 없었지만, 한 시트에 `padding`과
  `padding-top`이 같이 있으면 **어느 쪽이 이기는지가 캐스케이드 질문**이고
  전개 없이는 답할 수 없다. 렌더링 관심사가 아니다
- **기본값은 채우지 않는다.** 결과 맵에 없으면 기본값이다. 기본값 표는
  Phase 4가 갖는다 — `flex-direction`이 `column`이라는 걸 알아야 하는 쪽이 거기다

---

## Phase 4 — Yoga 레이아웃 + DOM 페인팅

**목표:** 드디어 화면에 나온다.

- [x] `yoga-layout` 연동 (WASM 로드 포함) — `yoga-layout/load` 경유, `loadLayoutEngine()`
- [x] 모델 → Yoga 노드 트리 변환
- [x] 레이아웃 계산 후 좌표 회수
- [x] 좌표에 맞춰 절대위치 DOM 생성
- [x] USS 시각 속성 → CSS 매핑 (`src/render/css-map.ts`)
- [x] 모든 요소에 `box-sizing: border-box`
- [x] **z-index를 출력하지 않음** (형제 순서에 맡김) — 테스트로 고정
- [x] `resolveAsset` 훅 + 플레이스홀더 폴백
- [x] 리렌더 시 Yoga 노드 해제 (누수 테스트 통과)
- [x] 놀이터에서 실제로 보임 — 브라우저 육안 확인 완료 (2026-08-02 22:06)

> 여기서 컨트롤을 더 추가하고 싶어지는데, **참는다.** v0.1은 3종이다.

**완료 (2026-08-02).** 테스트 157개. 레이아웃 23개, 페인팅 17개가 새로 붙었다.

### 코드를 쓰기 전 확인해서 잡은 것

- **`render()`는 동기인데 Yoga 로드는 비동기다.** 기본 엔트리 `yoga-layout`은
  top-level await을 쓴다. `yoga-layout/load`는 안 쓰므로 그쪽을 경유해서
  모듈 그래프를 동기로 유지하고, `await loadLayoutEngine()`을 한 번 부르게 했다
- **Yoga 기본값에 기대면 안 된다.** `flex-direction`(column)과 `box-sizing`(border-box)은
  마침 USS와 같지만 **`flex-shrink`는 Yoga가 0, USS 문서는 1**이다.
  지원하는 레이아웃 속성은 전부 명시적으로 쓴다
- **`getInstanceCount`가 없다.** 누수를 Yoga에게 물어볼 수 없어서 `liveNodeCount()`로
  직접 센다
- `setPositionType(absolute)`에 0을 넣을 뻔했다(정답 2). enum을 import해서 쓰고
  숫자를 코드에 적지 않는다

### 브라우저 확인 (2026-08-02 완료)

놀이터 샘플이 의도대로 나왔고, `flex-direction: row`를 지우니 세로로 쌓였다 —
기본값 `column`이 재현된다. **사용자의 실제 졸업작품 UXML/USS도 제대로 렌더됐다.**

### 남은 확인

- 텍스트 측정 기본 구현이 캔버스 기반이라 플랫폼·폰트에 따라 흔들린다.
  Phase 5는 반드시 `measureText`를 주입해서 고정해야 한다
- 행 높이를 `font-size * 1.2`로 잡았다. 유니티와 다를 가능성이 높고 Phase 5에서 맞춘다

---

## Phase 5 — 골든 테스트 체계

**목표:** "유니티랑 진짜 똑같이 나오냐"에 숫자로 답할 수 있는 상태.
공개했을 때 첫 번째로 받는 질문이 이것이고, 답이 없으면 아무도 안 쓴다.

- [x] 케이스 세트 작성 — 20개 (`tests/golden/cases.ts`)
- [x] 웹 렌더 결과 자동 캡처 — 좌표. 브라우저 불필요
- [x] diff 측정 및 임계값 판정 — 요소별 좌표, 허용 오차 0.5px
- [x] `pnpm test:golden` 동작 — 회귀 20개 통과, 정확도 18개는 기준값 대기
- [x] `docs/accuracy.md` 작성 — **수치는 아직 "미측정"**
- [x] **유니티 기준값 확보** — 18/18 (2026-08-02)
- [x] 정확도 수치 기록 — **케이스 17/18, 값 242/244 (99.2%)**

**완료 (2026-08-02).** 전체 테스트 196개.

미검증 가정 네 개가 전부 "맞음"으로 판정됐다 — `flex-shrink` 기본값 1,
우선순위 트리플 계산, 상속보다 직접 지정 우선, 인라인 최우선.
특히 `flex-shrink`는 Yoga 엔진 기본값(0)을 그대로 뒀으면 틀렸을 자리이고,
**"지원 속성을 전부 명시적으로 쓴다"는 Phase 4의 결정이 막았다.**

불일치 1건: 크기 미지정 부모에 대한 `%`. **유니티가 옳고 우리가 틀렸다.**
`yoga-layout` 3.2.1과 UI Toolkit이 벤더링한 Yoga의 동작 차이이고, 설정으로 맞출 수
없다. 손으로 보정하는 것은 절대 규칙 1 위반이므로 `KNOWN_DIVERGENCES`에 정확히
두 값으로 등록했다. 사유와 실패한 시도 전부 `docs/accuracy.md`에 있다.

> **픽셀 diff에서 좌표 diff로 바꿨다.** 유니티는 자기 폰트 에셋으로, 브라우저는
> `sans-serif`로 글자를 그린다. 레이아웃이 100% 정확해도 글자 픽셀은 전부 다르므로,
> 텍스트가 든 케이스에서 픽셀 수치는 레이아웃이 아니라 폰트 차이를 잰다.
> 답해야 할 질문("내 레이아웃이 유니티와 같은 자리에 오나")은 좌표로 답한다.
> `pixelmatch`·`pngjs`는 시각 속성 대조용으로 남겨둔다.

> **스크린샷 대신 좌표 JSON을 뽑는다.** 원안은 수동 스크린샷이었지만,
> 패널을 순회하며 요소별 `worldBound`를 JSON으로 쓰는 에디터 스크립트는
> 배치모드 파이프라인이 아니라 스크립트 하나이고, 눈으로 좌표를 읽는 것보다
> 정확하며 케이스가 늘어도 비용이 그대로다.

> **기준값이 없으면 실패가 아니라 건너뛴다.** 아무도 생성할 수 없는 파일이
> 없다고 CI가 깨지면 안 되고, 동시에 미측정이 통과로 보여서도 안 된다.
> 테스트가 확보율(`0/18`)을 매번 출력한다.

### 케이스 세트

```
실패 패턴 검증 ★ 최우선
  flex-direction 미지정 (기본값이 column인지)   ← 1순위 실패 원인
  형제 순서에 따른 겹침 (z-index 없이)
  부모에 크기 없는 상태의 % 자식 (계산 안 되는 것이 정상)
  margin 상쇄 없음
  시작값이 auto인 속성의 트랜지션 (작동 안 하는 것이 정상)

레이아웃 기초
  flex-direction: row / column (+ reverse)
  justify-content 전체 값
  align-items 전체 값
  flex-wrap

크기
  flex-grow / flex-shrink / flex-basis 조합
  퍼센트 vs 픽셀
  min/max width·height 충돌
  width 미지정 시 콘텐츠 기반 크기

박스 모델
  padding + border + width 동시 지정   ← border-box 검증 핵심

위치
  position: absolute 오버레이
  top/left/right/bottom 조합
  중첩 absolute

텍스트
  줄바꿈
  text-overflow: ellipsis
  font-size 상속
  -unity-text-align 세로+가로 조합
  -unity-font-style 4종

Unity 고유 속성
  -unity-slice-* (9-slice)
  -unity-background-image-tint-color
  -unity-text-outline-*

스타일 우선순위
  동일 우선순위 셀렉터 충돌
  상속 vs 직접 지정
  인라인 오버라이드
```

> 유니티 스크린샷은 **수동으로 찍는다.** 배치모드 파이프라인을 짜는 건
> 이 단계에서 배보다 배꼽이 크다. 케이스 30개면 손으로 찍는 게 빠르다.

---

## Phase 6 — 놀이터 완성 + v0.1 공개

- [x] 놀이터에 예제 프리셋 드롭다운 — 6개, 함정 위주로 선정
- [x] 경고/에러 표시 영역
- [x] URL 공유 기능 — 해시에 문서와 패널 크기를 실음
- [x] GitHub Pages 배포 — <https://reuhomi.github.io/uxml-preview/>
- [x] README에 정확도 수치
- [x] README에 데모 GIF — 3종 (flex-direction / round-trip / panel-size)
- [x] `docs/uss-vs-css.md` 작성 (독립 SEO 자산) — 영/한, 함정마다 놀이터 링크
- [x] `docs/supported.md` 완성 — 영어판 포함
- [x] **npm 배포 — `uxml-preview@0.1.0` (2026-08-03)**
- [ ] GitHub Topics 설정 — 문구 준비됨, `gh` CLI가 없어 수동
- [x] v0.1.0 릴리스 노트 — `CHANGELOG.md`

---

## v0.1 완료 (2026-08-03)

Phase 0~6을 마쳤다. 테스트 228개, 유니티 6000.0.40f1 대조 좌표 242/244.
놀이터 <https://reuhomi.github.io/uxml-preview/>, 패키지 `uxml-preview@0.1.0`.

남은 것은 저장소 설정(Topics) 하나와 홍보뿐이고, 둘 다 코드가 아니다.

### 계획에 없었지만 넣은 것

- **놀이터 패널 크기 지정.** 창을 따라가면 `%`와 stretch가 창에 걸려서
  "1920x1080에서 어떻게 보이나"에 답할 수 없다. 프리뷰를 여는 이유의 절반이 그건데
- **`round-trip: exact` 실시간 표시.** 놀이터가 1순위 기능을 전혀 안 보여주고 있었다
- **`THIRD-PARTY-NOTICES.md`.** 사이트 번들이 yoga를 인라인하므로 MIT 재배포에
  해당하는데 고지가 없었다

---

## Phase 7+ — v0.1 이후

v0.1을 내고 반응을 본 뒤에 구체화한다.

**Phase 7 — 컨트롤 확장**
ScrollView, TextField, Toggle, Slider, Foldout, DropdownField /
`<ui:Template>`·`<ui:Instance>` 전개 / pseudo-class 상태 전환

**Phase 8 — 편집 레이어**
엘리먼트 선택·아웃라인 / 속성 인스펙터 / write-back
(Phase 1의 출처 정보가 여기서 빛을 발한다)

**Phase 9 — 외부 도구 통합**
Open Design 플러그인 / `.uxml` 프리뷰 분기 / 에이전트용 SKILL.md
